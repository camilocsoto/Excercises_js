var edad = 0 ;

if (edad < 18) {
    console.log("El usuario no puede votar")
} else if (edad == 18) {
    console.log("El usuario podrá votar por primera vez")
} else {
    console.log ("El usuario podrá votar otra vez")
}

/**Hacer un condicional if en una linea*/

condicional ? true : false;

// validar si es un 1

var num = 1; 

let resultado = num === 1 ? "Es igual a 1":"No es 1";
//guardar cond = condicion ? true ; false

console.log(resultado)
//imprimir en consola
